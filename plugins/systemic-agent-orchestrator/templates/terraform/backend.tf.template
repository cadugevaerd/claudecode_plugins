# S3 Backend Configuration
# REQUIRED: S3 state storage for production agents

terraform {
  backend "s3" {
    bucket         = "terraform-state-{account_id}"
    key            = "agents/{project_name}/terraform.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "terraform-locks"
  }
}
