---
description: Commit e push rápido com validação mínima
allowed-tools: Bash(git:*)
model: claude-sonnet-4-5
---

# Quick Commit - Commit e Push Rápido

Executa commit + push rápido com validação de segurança essencial,
sem testes ou verificações completas.

## 🎯 Objetivo

- Executar commit e push rápido (< 1 minuto)
- Validar segurança essencial (arquivos sensíveis)
- Gerar mensagem de commit conveniente
- Sincronizar com remote via rebase

## Procedimento de Execução

### 1. Validar Segurança

Verifique se há arquivos sensíveis usando `git status --short`:

- `.env`, `.env.*`, `*.pem`, `*.key`, `*.pfx`
- `credentials.*`, `secrets.*`
- Arquivos contendo `password`, `token`, `api_key`, `api-key`

**Se houver arquivo sensível**: PARE imediatamente e exiba:

```text
⚠️  ALERTA DE SEGURANÇA

Arquivos sensíveis detectados:
- .env
- credentials.json

❌ Quick commit BLOQUEADO

Soluções:
1. Adicione ao .gitignore: echo ".env" >> .gitignore
2. Remove do stage: git rm --cached <arquivo>
3. NUNCA commit arquivos sensíveis!
```

### 2. Mostrar Resumo de Mudanças

Execute `git diff --stat` e exiba:

```text
📊 Mudanças detectadas:
- 3 arquivos modificados
- +45 linhas adicionadas
- -12 linhas removidas
```

### 3. Gerar Mensagem de Commit (Automática)

**SEMPRE auto-gere a mensagem** baseada nos arquivos modificados.

Execute `git diff --name-only` para listar arquivos modificados e analise:

**Regras de geração (Conventional Commits):**

- `docs: update <contexto>` se maioria arquivos `.md`
- `chore: update <contexto>` se maioria arquivos de configuração (`.json`, `.yaml`, `.yml`)
- `test: update <contexto>` se maioria arquivos de teste
- `style: format <contexto>` se maioria arquivos de estilo
- `feat: add <funcionalidade>` se novos recursos
- `fix: correct <problema>` se correções
- `refactor: improve <componente>` se refatorações
- `chore: update <nome-arquivo>` se arquivo único
- `chore: update <N> files` se múltiplos arquivos sem padrão claro

**⚠️ RESTRIÇÕES CRÍTICAS:**

- ❌ NUNCA mencione "Claude Code", "Claude", "AI assistant" ou qualquer ferramenta de IA
- ❌ NUNCA use termos como "with AI help", "automated by", "generated by"
- ✅ Use linguagem neutra focada no que foi feito, não em quem/como fez

**Exemplos válidos:**

- `docs: update README with installation steps`
- `chore: update config files`
- `test: add unit tests for auth module`
- `feat: add user authentication`
- `fix: correct validation logic`

**Exemplos INVÁLIDOS (nunca use):**

- ❌ `docs: update README with Claude Code integration`
- ❌ `feat: add feature with AI assistance`
- ❌ `chore: automated update by Claude`

### 4. Executar Commit e Push

Execute em sequência:

```bash
git add -A
git commit -m "mensagem"
git fetch origin
git pull --rebase origin $(git branch --show-current)
git push origin $(git branch --show-current)
```

**Se houver conflitos durante rebase**:

```text
⚠️  CONFLITOS DETECTADOS

Arquivos em conflito:
- src/main.py
- config.json

🛑 Resolva manualmente:

1. Edite arquivos e resolva conflitos
2. git add <arquivos-resolvidos>
3. git rebase --continue
4. git push

Ou cancele: git rebase --abort
```

### 5. Exibir Resultado Final

```text
═══════════════════════════════════════════
⚡ QUICK COMMIT CONCLUÍDO!
═══════════════════════════════════════════

📊 RESUMO:
├─ Arquivos: 3 modificados
├─ Mudanças: +45 / -12 linhas
├─ Commit: abc1234 chore: update config files
├─ Branch: main
└─ Push: ✅ REALIZADO

═══════════════════════════════════════════
```

## ✅ Critérios de Sucesso

- [ ] Nenhum arquivo sensível detectado
- [ ] Mudanças resumidas apresentadas ao usuário
- [ ] Mensagem de commit auto-gerada (NUNCA menciona Claude Code ou IA)
- [ ] Mensagem segue Conventional Commits
- [ ] git add -A executado com sucesso
- [ ] Commit criado com sucesso
- [ ] Rebase com origin realizado sem conflitos
- [ ] Push para remote concluído
- [ ] Resultado final exibido com resumo completo

## 📝 Exemplos de Uso

### Exemplo 1: Atualização de documentação

```bash
/quick-commit
# → Analisa mudanças (README.md modificado)
# → Gera: "docs: update README with usage examples"
# → Commit e push realizados
```

### Exemplo 2: Múltiplos arquivos de configuração

```bash
/quick-commit
# → Analisa mudanças (package.json, tsconfig.json modificados)
# → Gera: "chore: update config files"
# → Commit e push realizados
```

### Exemplo 3: Bloqueio por arquivo sensível

```bash
/quick-commit
# → ⚠️ ALERTA: .env detectado
# → Comando BLOQUEADO
```

## Segurança

- ✅ SEMPRE verifica arquivos sensíveis (BLOQUEADOR)
- ✅ Valida acesso Git básico
- ❌ NÃO executa testes
- ❌ NÃO valida linters
- ❌ NÃO verifica CI/CD
- ❌ NÃO analisa documentação

Você é responsável por garantir que as mudanças são seguras e testadas localmente.

______________________________________________________________________

Para documentação completa (quando usar, exemplos avançados,
troubleshooting), consulte o README do plugin.
